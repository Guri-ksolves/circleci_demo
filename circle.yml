machine:
  ruby:
    version: 2.4.0

general:
  artifacts:
    - "~/circleci_demo/log/"

test:
  pre
  post
  override: echo "qwert"



deployment:
  production:
    branch: master
    # heroku:
    #   appname: circleci-demo-app
    commands:
      - ssh ...
